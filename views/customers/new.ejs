<!DOCTYPE html>
<html lang="pt">
<head>
    <title>Novo Cliente</title>
</head>
<body>
    <h2>Cadastrar Cliente</h2>
    <% if (messageError) { %>
        <p style="color: red;"><%= messageError %></p>
    <% } %>
    <% if (messageSuccess) { %>
        <p style="color: green;"><%= messageSuccess %></p>
    <% } %>
    <form action="/customers" method="POST">
        <h3>Dados Pessoais</h3>
        <input type="text" name="name" placeholder="Nome" required value="<%= formData.name || '' %>">
        <input type="text" name="document" placeholder="Documento" required value="<%= formData.document || '' %>">
        <input type="email" name="email" placeholder="E-mail" required value="<%= formData.email || '' %>">
        <input type="text" name="phone_number" placeholder="Telefone" required value="<%= formData.phone_number || '' %>">
        <input type="text" name="alt_phone_number" placeholder="Telefone Alternativo" value="<%= formData.alt_phone_number || '' %>">

        <h3>Endereço</h3>
        <input type="text" name="postal_code" placeholder="CEP" required value="<%= formData.postal_code || '' %>">
        <input type="text" name="street" placeholder="Rua" required value="<%= formData.street || '' %>">
        <input type="text" name="number" placeholder="Número" required value="<%= formData.number || '' %>">
        <input type="text" name="neighborhood" placeholder="Bairro" required value="<%= formData.neighborhood || '' %>">
        <input type="text" name="state" placeholder="Estado" required value="<%= formData.state || '' %>">

        <button type="submit">Cadastrar</button>
    </form>
    <a href="/dashboard">Voltar</a>
</body>
</html>
